#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 *
 * Return: 0 if successful
 */

int main(void)
{
	int n;

	strand(time(0));
	n = rand() - RAND_MAX / 2
		if (n > 0)
		{
			print("%d is positive\n", n);
		}
		else if (n < 0)
		{
			printf("%d is negative\n", n);
		}
		else
		{
			printf("%d zero\n"' n);
		}
	return (0)
}
